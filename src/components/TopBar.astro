---
import themeIcon from "../assets/theme.svg?raw";
---

<div
    id="container"
    class="flex flex-row w-full bg-topbar-bg fixed top-0 left-0 right-0 z-50 backdrop-blur-sm"
>
    <div id="logo"></div>
    <div id="menu" class="flex w-full px-10 lg:px-12 items-center text-md">
        <ul
            class="flex flex-row justify-around w-full gap-8 text-font-primary text-sm"
        >
            <li><a href="#profile">@TonnyOrtiz</a></li>
            <li><a href="#stack">Stack</a></li>
            <li><a href="#projects">Projects</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </div>

    <div
        id="theme-toggle"
        class="flex px-4 md:px-8 py-2 justify-end items-center text-font-primary relative"
    >
        <button
            id="theme-toggle-btn"
            class="p-2 rounded-full hover:bg-slate-200 dark:hover:bg-slate-800 transition-colors flex flex-row items-center gap-2"
            aria-label="Toggle Theme Dropdown"
            aria-expanded="false"
        >
            <!-- Static Theme Settings Icon -->
            <span
                class="w-6 h-6 block [&>svg]:w-full [&>svg]:h-full"
                set:html={themeIcon}
            />
            <span class="text-sm whitespace-nowrap hidden md:block">
                Display Mode</span
            >
        </button>
    </div>

    <script>
        const getSystemTheme = () =>
            window.matchMedia("(prefers-color-scheme: dark)").matches
                ? "dark"
                : "light";

        const updateThemeUI = (themeSetting: string) => {
            // 1. Update the document class
            const isDark =
                themeSetting === "auto"
                    ? getSystemTheme() === "dark"
                    : themeSetting === "dark";
            document.documentElement.classList.toggle("dark", isDark);

            // 2. Highlight the active option in the dropdown
            document.querySelectorAll(".theme-option").forEach((btn) => {
                if (btn instanceof HTMLElement) {
                    if (btn.dataset.theme === themeSetting) {
                        btn.classList.add("text-accent-blue");
                    } else {
                        btn.classList.remove("text-accent-blue");
                    }
                }
            });
        };

        const applyTheme = (themeSetting: string) => {
            if (!document.startViewTransition) {
                updateThemeUI(themeSetting);
            } else {
                document.startViewTransition(() => updateThemeUI(themeSetting));
            }

            if (themeSetting === "auto") {
                localStorage.removeItem("theme");
                // Update theme based on system preference
                updateThemeUI(getSystemTheme());
            } else {
                localStorage.setItem("theme", themeSetting);
            }
        };

        // Initialize state on load
        const initTheme =
            typeof localStorage !== "undefined" && localStorage.getItem("theme")
                ? localStorage.getItem("theme")
                : "auto";
        updateThemeUI(initTheme || "auto");

        // Toggle dropdown
        const toggleBtn = document.getElementById("theme-toggle-btn");
        const dropdown = document.getElementById("theme-dropdown");

        toggleBtn?.addEventListener("click", (e) => {
            e.stopPropagation();
            dropdown?.classList.toggle("hidden");
        });

        // Click outside to close
        document.addEventListener("click", (e) => {
            if (
                toggleBtn &&
                dropdown &&
                !(e.target instanceof Node && toggleBtn.contains(e.target)) &&
                !(e.target instanceof Node && dropdown.contains(e.target))
            ) {
                dropdown.classList.add("hidden");
            }
        });

        // Theme options clicks
        document.querySelectorAll(".theme-option").forEach((btn) => {
            btn.addEventListener("click", (e) => {
                const target = e.currentTarget;
                if (target instanceof HTMLElement) {
                    const selectedTheme = target.dataset.theme;
                    if (selectedTheme) {
                        applyTheme(selectedTheme);
                    }
                }
                dropdown?.classList.add("hidden");
            });
        });

        // Listen for OS theme changes if on "auto" mode
        window
            .matchMedia("(prefers-color-scheme: dark)")
            .addEventListener("change", () => {
                if (!localStorage.getItem("theme")) {
                    applyTheme("auto");
                }
            });
    </script>
</div>
